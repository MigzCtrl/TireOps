/**
 * Database Query Layer Exports
 *
 * This module provides a centralized export point for all database queries.
 * Import queries from here for consistency and easier refactoring.
 *
 * @example
 * ```ts
 * import { customerQueries, vehicleQueries } from '@/lib/queries';
 * ```
 */

export * as customerQueries from './customer.queries';
export * as vehicleQueries from './vehicle.queries';
