/**
 * Service Layer Exports
 *
 * This module provides a centralized export point for all business logic services.
 * Import services from here for consistency and easier refactoring.
 *
 * @example
 * ```ts
 * import { customerService, vehicleService } from '@/lib/services';
 * ```
 */

export * as customerService from './customer.service';
export * as vehicleService from './vehicle.service';
